# PostgreSQL
# Local:
DATABASE_URL=postgresql://usuario:pass@localhost:5432/pifaucet
# Railway: NO pegues la URL a mano en el repo. En el servicio WEB añade
# Variable Reference → servicio Postgres → DATABASE_URL

# Firma de JWT (genera una cadena larga aleatoria en producción)
JWT_SECRET=cambia-esto-por-al-menos-32-caracteres-aleatorios

# Puerto local (Railway inyecta PORT automáticamente)
# PORT=3000
